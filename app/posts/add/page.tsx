import Form from "@/components/posts/Form";
import React from "react";

export const metadata = {
  title: "Add Post | MyApp",
  description: "Add post page",
};

export default function Add() {
  return <Form />;
}
